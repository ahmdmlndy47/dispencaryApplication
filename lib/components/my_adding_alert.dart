import 'package:flutter/material.dart';
class MyAddingAlert extends StatelessWidget {
  const MyAddingAlert({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
